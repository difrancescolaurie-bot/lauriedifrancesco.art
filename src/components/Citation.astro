---
interface Props {
  author?: string;
  note?: string;        // note secondaire en petit italique (ex: "free translation from the French")
  hr?: boolean;         // affiche le trait horizontal (défaut: true)
}

const { author, note, hr = true } = Astro.props;
---

<div class:list={['epi-wrap', { 'epi-wrap--hr': hr }]}>
  <blockquote>
    <slot />
  </blockquote>
  {author && (
    <cite>
      {author}
      {note && <span class="epi-tradfr">{note}</span>}
    </cite>
  )}
</div>

<style is:global>
  .epi-wrap {
    border-left: 1px solid var(--brume);
    padding: .2rem 0 1.8rem 1.3rem;
    max-width: 60ch;
  }

  /* Le trait horizontal est un ::after ancré dans l'élément :
     aucun margin externe sur .epi-wrap ne peut créer d'espace entre le
     border-left et le trait, garantissant qu'ils se rejoignent toujours. */
  .epi-wrap--hr {
    padding-bottom: 0;
  }
  .epi-wrap--hr::after {
    content: '';
    display: block;
    width: var(--horizon-width, 64px);
    height: var(--horizon-height, 1.5px);
    background: var(--brume);
    margin-top: 1.8rem;
    /* aligne le trait avec le bord gauche du border-left */
    margin-left: calc(-1.3rem - 1px);
  }

  .epi-wrap blockquote {
    font-family: var(--font-body);
    font-weight: 300;
    font-style: italic;
    font-size: var(--t-m);
    line-height: 1.85;
    color: var(--ardoise);
  }

  .k {
    font-weight: inherit;
  }

  .epi-wrap cite {
    display: block;
    margin-top: .9rem;
    font-style: normal;
    font-size: var(--t-xs);
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--cerule);
  }

  .epi-tradfr {
    display: block;
    font-size: .62rem;
    letter-spacing: .08em;
    text-transform: none;
    font-style: italic;
    color: var(--ardoise);
    margin-top: .2rem;
  }
</style>
